name: 'Odoo Test & Code Climate'
description: 'Ejecuta tests de Odoo y reporta a Code Climate'
author: 'Chroma Agency'

inputs:
  odoo_version:
    description: 'Versión de Odoo'
    required: true
    default: '16.0'
  modules:
    description: 'Módulos a probar'
    required: true
  pg_version:
    description: 'Versión de Postgres'
    required: false
    default: '14'
  test_tags:
    description: 'Tags de test opcionales'
    required: false
    default: ''
  extra_addons_path:
    description: 'Rutas adicionales para --addons-path dentro del contenedor (separadas por comas)'
    required: false
    default: ''
  # NUEVOS INPUTS PARA ENTERPRISE
  enterprise_deploy_key:
    description: 'GitHub Token con acceso a odoo/enterprise'
    required: false
    default: ''
  # INPUT PARA CODE CLIMATE
  qlty_coverage_token:
    description: 'Token de Code Climate para reportar coverage'
    required: false
    default: ''
  coverage_branch:
    description: 'Branch de cobertura para Code Climate'
    required: false
    default: ''

runs:
  using: "composite"
  steps:

    # 1. Infraestructura (Red + DB)
    - name: Crear red Docker
      shell: bash
      run: docker network create odoo-test-network || true
    # 1. Cargamos la llave SSH en el agente
    - name: Setup SSH Agent
      if: ${{ inputs.enterprise_deploy_key != '' }}
      uses: webfactory/ssh-agent@v0.8.0
      with:
        ssh-private-key: ${{ inputs.enterprise_deploy_key }}
    # NUEVO: Clonar Odoo Enterprise
    - name: Clonar Odoo Enterprise
      if: ${{ inputs.enterprise_deploy_key != '' }}
      shell: bash
      run: |
        git clone --depth 1 --branch ${{ inputs.odoo_version }} \
          git@github.com:ChromaAgency/odoo-enterprise.git \
          ${{ github.workspace }}/enterprise

    - name: Iniciar Postgres
      shell: bash
      run: |
        docker rm -f db-postgres || true
        docker run -d --name db-postgres --network odoo-test-network \
          -e POSTGRES_USER=odoo -e POSTGRES_PASSWORD=odoo -e POSTGRES_DB=postgres \
          postgres:${{ inputs.pg_version }}

    - name: Esperar a Postgres
      shell: bash
      run: until docker exec db-postgres pg_isready -U odoo; do sleep 2; done

    - name: Ajustar permisos
      shell: bash
      run: chmod -R 777 .

    # 2. Ejecutar Tests (Generando coverage.xml)
    - name: Ejecutar Tests Odoo
      uses: addnab/docker-run-action@v3
      with:
        image: odoo:${{ inputs.odoo_version }}
        options: >-
          --user root
          -v ${{ github.workspace }}:/mnt/extra-addons
          -v ${{ github.workspace }}/enterprise:/mnt/enterprise
          --network odoo-test-network
          -e HOST=db-postgres
          -e USER=odoo
          -e PASSWORD=odoo
          -e INPUT_MODULES=${{ inputs.modules }}
          -e INPUT_TAGS=${{ inputs.test_tags }}
          -e INPUT_EXTRA_ADDONS_PATH=${{ inputs.extra_addons_path }}
          -e INPUT_QLTY_COVERAGE_TOKEN=${{ inputs.qlty_coverage_token }}
        run: |
          set -e
          apt-get update && apt-get install -y --no-install-recommends git
          if [ -f "/mnt/extra-addons/requirements.txt" ]; then
            echo "📦 Instalando dependencias desde requirements.txt..."
            python3 -m pip install  -r /mnt/extra-addons/requirements.txt || python3 -m pip install -r /mnt/extra-addons/requirements.txt --break-system-packages
          fi
          cd /mnt/extra-addons
          git config --global --add safe.directory /mnt/extra-addons

          git submodule update --init --recursive || echo "No hay submódulos o ya están actualizados"
          if [ -n "$INPUT_QLTY_COVERAGE_TOKEN" ]; then python3 -m pip install coverage || python3 -m pip install coverage --break-system-packages; fi

          TAGS_FLAG=""
          if [ -n "$INPUT_TAGS" ]; then TAGS_FLAG="--test-tags=$INPUT_TAGS"; fi

          ADDONS_PATH="/mnt/extra-addons"
          if [ -d "/mnt/enterprise" ]; then ADDONS_PATH="$ADDONS_PATH,/mnt/enterprise"; fi
          if [ -n "$INPUT_EXTRA_ADDONS_PATH" ]; then ADDONS_PATH="$ADDONS_PATH,$INPUT_EXTRA_ADDONS_PATH"; fi

          CMD="odoo -i $INPUT_MODULES --test-enable $TAGS_FLAG --stop-after-init --database=test_db --log-level=test --db_host=db-postgres --db_port=5432 --db_user=odoo --db_password=odoo --addons-path=$ADDONS_PATH"

          if [ -n "$INPUT_QLTY_COVERAGE_TOKEN" ]; then
            echo "🧪 Corriendo con Coverage..."
            export COVERAGE_FILE="/mnt/extra-addons/.coverage"
            python3 -m coverage run --source=/mnt/extra-addons --omit=*/__manifest__.py,*/tests/* /usr/bin/odoo -i $INPUT_MODULES --test-enable $TAGS_FLAG --stop-after-init --database=test_db --log-level=test --db_host=db-postgres --db_port=5432 --db_user=odoo --db_password=odoo --addons-path=$ADDONS_PATH
            python3 -m coverage xml -o /mnt/extra-addons/coverage.xml
            python3 -m coverage html -d /mnt/extra-addons/coverage-html
            ls -la /mnt/extra-addons/.coverage /mnt/extra-addons/coverage.xml /mnt/extra-addons/coverage-html/index.html
          else
            echo "🚀 Corriendo sin Coverage..."
            $CMD
          fi

    - name: Upload coverage artifacts
      uses: actions/upload-artifact@v4
      with:
        name: coverage-report-xml
        path: ${{ github.workspace }}/coverage.xml
        retention-days: 2

    - name: Upload coverage artifacts html
      uses: actions/upload-artifact@v4
      with:
        name: coverage-report-html
        path: ${{ github.workspace }}/coverage-html
        retention-days: 2
        include-hidden-files: true

    - name: Instalar Qlty CLI
      uses: qltysh/qlty-action/install@v2.2.0

    - name: Upload a Qlty (forzar branch ${{ inputs.coverage_branch }})
      env:
              QLTY_COVERAGE_TOKEN: ${{ inputs.qlty_coverage_token }}
      shell: bash
      run: |
        if [ -z "${QLTY_COVERAGE_TOKEN:-}" ]; then
          echo "QLTY_COVERAGE_TOKEN no configurado, se omite upload"
          exit 0
        fi

        COVERAGE_BRANCH="${{ inputs.coverage_branch }}"
        FALLBACK_BRANCH="${{ inputs.odoo_version }}"
        TARGET_BRANCH="$FALLBACK_BRANCH"

        if [ -n "$COVERAGE_BRANCH" ] && git ls-remote --exit-code --heads origin "$COVERAGE_BRANCH" >/dev/null 2>&1; then
          TARGET_BRANCH="$COVERAGE_BRANCH"
        else
          echo "coverage_branch no existe (o está vacío), se usa fallback: $FALLBACK_BRANCH"
        fi

        qlty coverage publish --override-branch "$TARGET_BRANCH" --no-validate ${{ github.workspace }}/coverage.xml

